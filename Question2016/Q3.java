
package Question2016;
/*
import java.util.Random;
import java.util.Scanner;
public class Q3 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Random rand = new Random();
        System.out.print("\nEnter the number of random integer: ");
        int size = sc.nextInt();
        int [] num = new int [size];
        int [] rounded = new int [size];
        int maximum = 0;
        System.out.print("\nThe random integer :");
        for(int i = 0 ; i<num.length ; i++){
            num[i] = rand.nextInt(1001);
            System.out.print( num[i] + " ");
            if(num[i]>maximum){
                maximum = num[i];
            }
        }
        System.out.print("\nThe approximation of the integer to the nearest tenth : ");
        for(int i = 0 ; i<num.length ; i++){
            rounded[i] = (int)(Math.round(num[i]/10.0)*10);
            System.out.print(rounded[i] + " ");
        }
        System.out.print("\nThe random integer in reverse order: ");
        for(int i = num.length-1 ; i >=0 ; i--){
            System.out.print(num[i] + " ");
            
        }
    }
}
*/